# Coinbase BTC-USD price feed is fetched from the backend using the public REST API,
# so no market-data secret is required.

# WalletConnect project ID for the RainbowKit wallet connector.
VITE_WALLETCONNECT_PROJECT_ID=""

# Optional: persist the order book and positions on Vercel KV.
KV_REST_API_URL=""
KV_REST_API_TOKEN=""
